{
  "name": "whatsapp-crm",
  "version": "1.0.0",
  "description": "WhatsApp Med Spa CRM — n8n + Gemini AI + Supabase",
  "private": true,
  "scripts": {
    "backend": "npm run dev --prefix backend/api",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
